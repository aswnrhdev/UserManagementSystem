This repository houses a mini project, serving as a CRUD (Create, Read, Update, Delete) application designed to manage user systems efficiently. Built as a dynamic web application, it utilizes MongoDB for data storage, enabling seamless interaction for creating, reading, updating, and deleting user records.
